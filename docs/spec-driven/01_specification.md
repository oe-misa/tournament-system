# 01 Specification

## 目的

大会情報の公開、会員のログイン、大会エントリー、成績閲覧、プロフィール管理、年間登録、段位申請、日次おみくじを一元化する。
管理者は大会管理、成績入力、段位申請の承認/却下を行う。

## ユーザー種別

### 公開ユーザー

- 公開トップを閲覧できる。
- 会員ページからログインまたは新規登録へ進める。

### 会員

- ログイン後に会員ダッシュボードを利用できる。
- プロフィールを確認・更新できる。
- 年間登録を更新できる。
- 大会一覧・大会詳細を閲覧できる。
- 条件を満たす大会へエントリーできる。
- 自分の成績一覧を閲覧できる。
- 自分の段位以上へ段位申請できる。
- 段位申請履歴、ステータス、担当者、管理者コメントを閲覧できる。
- 1 日 1 回だけおみくじを引ける。

### 管理者

- 会員機能を利用できる。
- 管理ダッシュボードを利用できる。
- 大会を作成・編集・削除できる。
- 大会ごとの成績を入力・更新できる。
- 段位申請を一覧し、未処理申請を承認または却下できる。
- 段位申請の処理時に管理者コメントを残せる。
- 段位申請を承認した場合、対象ユーザーの段位を更新する。

## 機能仕様

### 認証

- Laravel Breeze の Web 認証を使用する。
- 未ログインで会員・管理者画面にアクセスした場合、ログイン画面へリダイレクトする。
- 管理者画面は `is_admin = true` のユーザーのみ利用できる。

### ダッシュボード

- 会員名、現在段位、年間登録期限を表示する。
- 年間登録が未登録または期限切れの場合、更新導線を表示する。
- 本日のおみくじ結果、またはおみくじを引くボタンを表示する。
- 会員情報、大会登録、大会結果、年間登録、段位申請、段位申請履歴への導線を表示する。
- 管理者の場合、管理メニューと未処理件数を表示する。

### プロフィール

- 氏名、メールアドレスを更新できる。
- 任意でパスワードを更新できる。
- 現在段位と年間登録期限を表示する。
- 段位変更はプロフィールでは行わず、段位申請から行う。
- アカウント削除時は現在パスワードを要求する。

### 年間登録

- 年間登録の年度期間は 4 月 1 日から翌 3 月 31 日までとする。
- 登録は 1 年度単位のみとし、複数年更新や任意期間更新はできない。
- 未登録または期限切れ会員が登録した場合、登録日に関係なく現在年度の 3 月 31 日を期限とする。
- 当年度登録済み会員は、3 月 10 日以降に翌年度分を更新できる。
- 当年度登録済み会員は、3 月 9 日以前に翌年度分を更新できない。
- 翌年度分まで登録済みの場合、重複更新はできない。
- 登録履歴は `memberships` に保存する。
- 現在有効な期限は `users.membership_expires_at` に保存する。

### 大会

- 会員は大会一覧を開催日順に閲覧できる。
- 会員は大会詳細を閲覧できる。
- 大会にはタイトル、説明、開催日、エントリー締切、定員、最低段位レベルを設定できる。
- 最低段位レベルは表示時に級/段ラベルへ変換する。

### 大会エントリー

- 会員は大会詳細からエントリーできる。
- 年間登録が未登録または期限切れの場合、エントリーできない。
- ユーザー段位レベルが大会の最低段位レベル未満の場合、エントリーできない。
- エントリー締切を過ぎている場合、エントリーできない。
- 定員が設定されていて満員の場合、エントリーできない。
- 同一ユーザー・同一大会の重複エントリーは作成しない。
- 既存エントリーがある場合は成功扱いで既存データを返す。

### 成績

- 会員は自分の成績のみ閲覧できる。
- 成績には大会、開催日、順位、スコアを表示する。
- 管理者は大会ごとにエントリー済み参加者の成績を入力・更新できる。

### 段位

- 段位は `ranks.level` の数値で管理する。
- 表示ラベルは `RankLabel` で変換する。
- 基本対応は以下とする。
  - 0: 無段（F〜E級）
  - 1: 初段（D級）
  - 2: 弐段（C級）
  - 3: 参段（B級）
  - 4〜10: A級（四段〜十段）

### 段位申請

- 会員は現在段位以上の段位へ申請できる。
- 現在段位より下の段位へは申請できない。
- 未処理の申請が既にある場合、新規申請できない。
- 申請時に備考を登録できる。
- 申請は `pending` として保存する。
- 会員は自分の申請履歴を確認できる。

### 段位申請管理

- 管理者は全申請を一覧できる。
- 未処理申請のみ承認・却下できる。
- 承認時は申請ステータスを `approved` にし、対象ユーザーの `rank_id` を更新する。
- 却下時は申請ステータスを `rejected` にする。
- 承認・却下時は担当管理者、処理日時、管理者コメントを保存する。

### おみくじ

- 会員は 1 日 1 回おみくじを引ける。
- 結果は `大吉/吉/中吉/小吉/凶` のいずれかとする。
- 同日に既に引いている場合、新しい結果は作成しない。
- 結果はダッシュボードに表示する。

### API

- Sanctum 認証済みユーザー向け API を提供する。
- API は現時点で会員向けの参照・登録系に限定する。
- API 対象:
  - 自分自身の取得
  - 大会一覧・詳細
  - 大会エントリー
  - 自分の成績一覧
  - 段位申請作成

## 未定義・今後検討

- 年会費決済、入金確認、承認制年間登録。
- 大会公開/非公開、会場、外部フォーム連携。
- エントリーキャンセル画面。
- 管理者による会員管理画面。
- 操作監査ログの永続化。
